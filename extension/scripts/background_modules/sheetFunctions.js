/**
 * class SheetManager
 * 
 *  Takes a URL as parameter
 * 
 * methods:
 * 
 *  launch() -> creates new tab containing sheet
 *       (check if there's already a tab with 
 *       the sheet open and if so, just use that)
 * 
 *  getCellValue(cellAddr) -> gets the value of the given
 *                       cell
 * 
 *  setCellValue(cellAddr, val) -> sets the value of 
 *                        the given cell
 * 
 */

/* Written primarily by ChatGPT :O */
function editGoogleSheet(url, cellAddr, value) {
    // https://developer.chrome.com/docs/extensions/reference/identity/ 
    // USE THIS API!!!
}

export {editGoogleSheet}